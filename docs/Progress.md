# Progress — 進行中タスク

進行中・未着手のタスクのみを置く（運用ルール: 完了したら本ファイルから消し、
重要な変更は `CHANGELOG.md`／`history/` へ、設計判断は `decisions/`（ADR）へ移す）。

凡例: 🚧進行中 / ⬜未着手 / 🟡要判断（ユーザー決裁待ち）

---

## 進行中

- 🚧 **初回ログイン時パスワード強制変更（オプション・既定 OFF）** —
  設定フラグは追加済。残: `user.must_change_password` 列＋マイグレーション、ログイン
  ゲート、フロント対応。既定 OFF のため未配線でも無害。影響小・作業中 → 後回し候補。

---

## 未着手

- 🟡 **重複ファイルの検出・削除機能** — GooglePhotos/ローカルに同一ファイルが複数ある
  ケースの掃除。画像ハッシュ管理＋「画面での重複検出・比較・削除」を検討中。
  方針確定後に ADR 化（破壊的操作のため安全策が要点）。影響中・作業大 → 後回し候補。

- ⬜ **重複判定の二重実装の解消** — `check_duplicate_media` が新実装失敗時に旧実装へ
  サイレントフォールバック。一本化したい（フォールバック除去＝新実装を全面信頼する判断）。

- ⬜ **originals 直接再構築 CLI** — inbox は取り込み後に削除されるため、DB 初期化後の
  メタデータ再生成は手作業。`MEDIA_ORIGINALS_DIRECTORY` を直接走査して再登録する
  冪等な CLI を用意したい。影響小・作業中。
