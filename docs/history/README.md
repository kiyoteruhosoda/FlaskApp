# history/

重要な変更の記録を時系列で残すディレクトリ。日々の細かな進捗は `Progress.md`、
リリース単位の要約は `CHANGELOG.md`、設計判断は `decisions/`（ADR）に置く。

ここには「後から経緯を追いたくなる規模の変更」を `YYYY-MM-DD-<topic>.md` で残す
（例: 大規模リファクタ、データ移行、互換性を壊す変更の背景と手順）。
