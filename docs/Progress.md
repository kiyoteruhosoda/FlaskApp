# Progress — 進行中タスク

進行中・未着手のタスクのみを表で管理する（完了したら本ファイルから消し、重要な変更は
`CHANGELOG.md`／`history/` へ、設計判断は `decisions/`（ADR）へ移す）。

| 優先 | # | 概要 | 状態 | 影響度 | 工数 |
|---|---|---|---|---|---|
| 1 | T2 | originals 直接再構築 CLI | ⬜未着手 | 小 | 小 |
| 2 | T3 | 初回ログイン時パスワード強制変更フローの配線（既定 OFF） | 🚧進行中 | 小 | 中 |
| 3 | T4 | 重複判定の二重実装の解消（サイレントフォールバック除去） | ⬜未着手 | 中 | 中 |

---

## 詳細

- **T3 パスワード強制変更** — 設定フラグ `REQUIRE_PASSWORD_CHANGE_ON_FIRST_LOGIN`（既定
  OFF）は追加済。残: `user.must_change_password` 列＋マイグレーション、ログインゲート、
  フロント。既定 OFF のため未配線でも無害。
- **T4 重複判定の二重実装** — `check_duplicate_media` が新実装失敗時に旧実装へサイレント
  フォールバック。フォールバック除去＝新実装を全面信頼する判断が必要。
