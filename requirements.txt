# 本番環境で必要な依存関係
pillow
pillow-heif
PyMySQL
click
cryptography
pyotp
python-dotenv
qrcode
requests
celery
redis
PyJWT
Markdown
python-dateutil
MarkupSafe
werkzeug>=3.0.0

# FastAPI（メインフレームワーク）
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
gunicorn>=22.0.0
pydantic[email]>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9
httpx>=0.27.0

# メール送信

# webauthn（パスキー）
webauthn

# ストレージバックエンド依存関係（常時インストール）
azure-storage-blob>=12.28.0
azure-core>=1.38.0

# 開発環境でのみ必要な依存関係
alembic
pytest
debugpy

# 将来対応予定のストレージバックエンド
# 実装完成後にコメントアウトを外して有効化する
#
# AWS S3対応（実装時に有効化）
# boto3>=1.34.0
# botocore>=1.34.0
#
# Google Cloud Storage対応（実装時に有効化）
# google-cloud-storage>=2.10.0
# google-cloud-core>=2.4.0
